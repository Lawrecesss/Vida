# Movie clips are copyrighted; the references are ours and the clips are not.
# Only the manifest and the hand-corrected .reference.srt files are committed.
clips/
*.mp4
*.mkv
*.mov
*.m4v
*.wav
*.flac
*.mp3
